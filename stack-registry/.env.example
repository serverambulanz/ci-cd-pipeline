# Harbor Registry Stack Environment Variables
# Diese Secrets müssen in Portainer eingetragen werden!

# Harbor Core Secret (generieren mit: openssl rand -hex 16)
HARBOR_CORE_SECRET=your_core_secret

# Harbor JobService Secret (generieren mit: openssl rand -hex 16)
HARBOR_JOBSERVICE_SECRET=your_jobservice_secret

# Harbor Registry Secret (generieren mit: openssl rand -hex 16)
HARBOR_REGISTRY_SECRET=your_registry_secret

# Harbor Registry Password (generieren mit: openssl rand -hex 16)
HARBOR_REGISTRY_PASSWORD=your_registry_password

# Harbor Database Password (generieren mit: openssl rand -hex 16)
HARBOR_DB_PASSWORD=your_db_password

# Trivy GitHub Token (optional, für höheres Rate Limit)
TRIVY_GITHUB_TOKEN=your_github_token_optional
